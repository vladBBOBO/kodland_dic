import random

s= "+-/*!&$#?=@abcdefghijklnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ1234567890"

len_pas = int(input("указать длину"))

password = ""

for x in range(len_pas):
  password += random.choice(list(s))

print(password)
